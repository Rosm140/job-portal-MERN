import api from "./axios";
export const userAPI = { getProfile: () => api.get("/users/profile"), updateProfile: (d) => api.put("/users/profile", d), uploadResume: (f) => { const fd = new FormData(); fd.append("resume", f); return api.post("/users/upload-resume", fd, { headers: { "Content-Type": "multipart/form-data" } }); }, deleteResume: () => api.delete("/users/resume"), changePassword: (d) => api.put("/users/change-password", d) };
